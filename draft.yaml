models:
  Models\Cadastros\Talhao:
    tenant_id: id foreign
    fazenda_id: id foreign
    uuid: uuid
    nome: string unique
    area_total: double:10,2
    bloco: string
    status: enum:ATIVO,DESATIVADO
    softDeletes: true
    timestamps: true