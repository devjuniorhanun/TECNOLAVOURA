models:
  Models\Cadastros\AnoAgricola:
    tenant_id: id foreign
    uuid: uuid
    nome: string unique
    data_abertura: date nullable
    data_fechamento: date nullable
    status: enum:ATIVO,DESATIVADO
    softDeletes: true
    timestamps: true